# api-codtech

# 🌦 Weather Forecast Dashboard (API Integration + Data Visualization)

## 📌 Overview
This project is part of my **CodTech internship task**.  
It demonstrates **API integration** with **OpenWeatherMap** to fetch **5-day weather forecasts** for any city, process the data, and display it in a **visual dashboard** using `Matplotlib`.

The dashboard includes:
- Temperature forecast (°C)
- Humidity forecast (%)
- Wind speed forecast (m/s)

---

## 🚀 Features
- Fetches real-time **5-day weather forecast** using OpenWeatherMap API.
- Visualizes the data in **three interactive line charts**.
- Supports **any city** (just change the `CITY` variable).
- Uses **metric units** for readability.

---

## 📂 Project Structure
